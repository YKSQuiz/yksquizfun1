# YKS Quiz Deployment Environment Variables
# Bu dosyayı .env olarak kopyalayın ve değerleri güncelleyin
# ⚠️  GÜVENLİK: Bu dosyayı git'e commit etmeyin!

# VPS Connection Settings
VPS_HOST=your_vps_host_here
VPS_USER=your_vps_username
VPS_PASS=your_secure_vps_password
VPS_EMAIL=your_email@example.com

# Git Settings (optional - defaults will be used if not set)
GIT_REPO=origin
GIT_BRANCH=main

# Firebase Service Account (for database scripts)
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/your/serviceAccountKey.json

# Android Keystore Passwords (for APK building)
KEYSTORE_PASSWORD=your_secure_keystore_password
KEY_PASSWORD=your_secure_key_password
